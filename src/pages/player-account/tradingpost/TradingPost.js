/* istanbul ignore file */
import React from 'react'
import { Layout } from 'providers/MainLayout'

const TradingPost = () => <Layout>{({ Content }) => <Content>Trading Post</Content>}</Layout>

export default TradingPost
